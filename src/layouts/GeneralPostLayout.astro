---
import MainLayout from "./MainLayout.astro";
import PostHeader from "../components/PostHeader.astro";
import { Image, Picture } from "@astrojs/image/components";


const { frontmatter } = Astro.props;
console.log(frontmatter); //This will be terminal of vscode

const { category, slug, title, slogan, note, date, image, description, playlist} = frontmatter;
// saves it from being {frontmatter.title} ...ect

---

<MainLayout title={title} description={slogan} note={note}>
	<PostHeader {title} {slogan} {date} {category} {playlist} {description} {image} {note} />
    <div class=`bg-slate-900 m-2 p-3 flex flex-col justify-evenly rounded-xl`>
            <slot>we ain't figured out this post yet</slot>
        </div>
</MainLayout>